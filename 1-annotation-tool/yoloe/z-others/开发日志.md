
1. 第一次迭代 [v0.1.0]

    问题点：
        a. 标签描述不对劲，仅仅是 "car object", "person in the scene" 什么的，应该没有出发 qwen 的描述
        b. 界面右侧的需要直接显示中文，落盘是英文就行
    修复：
        a. 根因：此前冒烟用了 `--no_llm`，写入了占位短语（error=no_llm）。现已：
           - 正式生成前检查 vLLM 健康，不可用则直接报错（禁止静默占位）
           - `describe` 自动识别并重写 no_llm/占位描述（成功标记 source=vllm）
           - 重跑：`bash 0-generate.sh --limit N`（不要加 --no_llm）；旧 caption 需加 `--force`
           - 已验证样例：`blue forklift` / `blue industrial vehicle` 等真实描述
        b. 打开图片时自动英→中并缓存到 draft；右侧主显中文，下方保留 EN 可编辑/落盘
           - 检查界面已重启：http://localhost:8082

2. 第二次迭代 [v0.1.1]

    问题点：
        a. 图像加载过慢，无法加载
    修复：
        a. 根因：打开图时 `get_item` 同步逐条调 vLLM 翻译，请求被卡住像“加载不出来”。
           - `/api/item` 改为纯读盘，立刻返回
           - 中文改由 `/api/ensure_zh` 后台批量翻译，完成后刷新右侧
           - 图像与 EN 文案先显示，状态栏显示「翻译中…」
           - 请硬刷新浏览器后再试 http://localhost:8082

3. 第三次迭代 [v0.1.2]

    问题点：
        a. 一些 bug（RGBA 存 JPEG；空 content 导致 strip 崩溃）
        b. qwen 描述的过程比较慢，有没有办法加速
    修复：
        a. crop 保存前转 RGB；空 content 容错；单目标失败跳过不中断整批
        b. 加速三板斧：
           - 关闭 Qwen3 thinking（`enable_thinking=false`）
           - describe 并发（默认 `--workers 8`，可调 12~16）
           - 精简 prompt + `max_tokens=256`
           续跑：`bash 0-generate.sh --stages describe,caption`
           （不要加 `--force`，会跳过已成功目标）

4. 第四次迭代 [v0.1.3]

    问题点：
        a. 右侧的 caption 没有用上描述词
        b. 我希望 captions 在界面下方，每点击一个，右侧就是不同目标的描述
    修复：
        a. 根因：caption 在占位描述阶段生成。现优先用 vllm 描述短语；检测到占位会自动重建
           （已重建完成，样例：white box truck / blue truck with open bed …）
        b. 布局：下方横滑 captions；右侧只显示当前 caption 关联目标，并标 “caption 使用: …”
           硬刷新 http://localhost:8082

5. 第五次迭代 [v0.1.4]

    问题点：
        a. 反过来吧，captions 在界面右侧，每点击一个，界面下方就是不同目标的描述
        b. 目标描述和缩略图放一栏，有间隔就行，主图部分留大一些
        c. captions 的中文又不见咧
    修复：
        a/b. 布局：主图加大；右侧 captions；下方横滑「缩略图+描述」；侧栏收窄
        c. caption 重建后 zh 被清空。现优先批量翻译 captions，打开/切换时自动补中文
           硬刷新 http://localhost:8082

6. 第六次迭代 [v0.1.5]

    问题点
        a. 问题点 5 指的是右下角那个全图缩略图，目标描述部分不用加缩略图
    修复：
        a. 已去掉目标描述里的 crop 缩略图；保留主图右下角全图小地图（minimap）
           硬刷新 http://localhost:8082

7. 第七次迭代 [v0.1.6]

    问题点：
        a. 好像还是有 crop 缩略图，不过好像这样才更合理，不需要更新这一点了
        b. 需要支持不同窗口之间的大小拉动
        c. 目标描述试试看隐藏EN部分
    修复：
        a. 保留目标 crop 缩略图（不改）
        b. 支持拖拽分隔条调整：左侧列表宽 / 右侧 caption 宽 / 下方目标区高（记住上次尺寸）
        c. 目标描述隐藏 EN，只显示中文 + 缩略图
           硬刷新 http://localhost:8082

8. 第八次迭代 [v0.1.7]

    问题点：
        a. 目标描述的窗口，目标名支持选择，选择后则更新当前 caption。可以去掉那一行小字 "caption 使用: white box truck"，直接使用哪个描述就用这样的绿色椭圆矩形框住就好
    修复：
        a. 底部目标改为可点选中文短语 chip；当前 caption 使用的短语绿色椭圆框高亮；点击其它短语则替换 caption 中对应英文并刷新旁译；去掉 "caption 使用:" 小字

9. 第九次迭代 [v0.1.8]

    问题点：
        a. 目标描述放界面右侧；captions 放下方，用子界面的方式放，参考 excel 表格那种，不过子界面按钮放caption上方一行，如 captions1 | captions2 | captions3
        b. caption中，目标描述用显眼的字体颜色，其他的可以保持白色
    修复：
        a. 右侧改为目标描述面板；底部 Excel 式标签 captions1|2|3… 切换子表
        b. caption 中/英文本中 grounding 短语用琥珀色加粗高亮，其余保持白色

10. 第十次迭代 [v0.1.9]

    问题点：
        a. 目标描述 和 caption 支持单个刷新，而非全部刷新
        b. 支持删除某个caption
        c. 支持删除目标框，但是需要弹窗确认
    修复：
        a. caption「刷新本条」只重写当前句（不再整图重建）；目标卡「刷新」仅重跑该 obj 描述
        b. 底部「删除本条」删除当前 caption，并重排 tabs
        c. 目标卡「删除」弹窗确认后移除框/描述/crop，并清理所有 caption 引用
    

11. 第十一次迭代 [v0.1.10]

    问题点：
        a. 目标描述支持多选
        b. 支持新增 caption，优先选 5 个初始 caption 外的目标
    修复：
        a. 短语 chip 改为多选切换（再点取消）；同一目标可同时选多条并写入 caption
        b. 「新增」优先采样尚未被已有 caption 覆盖的目标

12. 第十二次迭代 [v0.1.11]

    问题点：
        a. 目标描述那块有bug，会截取不是当前 caption 的目标
    修复：
        a. 右侧目标描述仅显示当前 caption 的 obj_ids，不再混入其它/未覆盖目标

13. 第十三次迭代 [v0.1.12]

    问题点：
        a. 支持隐藏可视化的标注框
        b. 只显示全图，取消右下角缩略图
        c. 主窗口支持 ctrl+鼠标滚轮缩放
    修复：
        a. 工具栏「标注框」开关显示/隐藏框与分割
        b. 移除右下角 minimap；默认全图适配
        c. Ctrl/⌘+滚轮以光标为中心缩放；拖拽可平移

14. 第十四次迭代 [v0.1.13]

    问题点：
        a. 拖动有点粘手，改成按住拖动，松开则释放
        b. 图像左上角支持显示当前进度和总数据量，如 12/100
        c. 支持 A 和 D 上一张下一张
        d. 左侧的文件概览放最右侧，增加子标签页，即  目标描述 | 文件概览
    修复：
        a. 平移改用 pointer capture：按住拖动、松开/cancel 立即释放
        b. 画布左上角显示 当前/总数（如 12/100）
        c. A/D 切换上一张/下一张（输入框内不触发）
        d. 左侧列表移到右侧子标签「目标描述 | 文件概览」

15. 第十五次迭代 [v0.1.14]

    问题点：
        a. 开启界面时，自动显示第一张，不要留黑
        b. 刷新目标描述时，不要同时刷新已选的其他目标的描述
    修复：
        a. 启动/选目录后自动 loadStem 第一张
        b. 单目标「刷新」只更新该 obj 描述，不再调用 ensureZhAsync 整表重载
    
16. 第十六次迭代 [v0.1.15]

    问题点：
        a. 点击刷新会直接删掉描述，不对劲
    修复：
        a. 根因1：刷新逻辑改写 caption 选中导致目标像被删；改为只更新该目标描述
        a. 根因2：ensureZhAsync 整表覆盖刚刷新结果；改为合并且跳过本地新刷新
        a. 失败/空结果回滚本地；落盘前校验，避免半成品覆盖

17. 第十七次迭代 [v0.2.0]

    问题点：
        a. 默认自动保存，切caption或者切图自动保存
        b. 参考 labelme 的页面左侧和上方来布局
        c. 自动检查当前文件夹中的数据结构规范，是否 `images/` + `jsons/`，json文件格式参考`/home/yulin/0-data/2-DroneObject/grounding_sample/rm/jsons`, 可以只有矩形框，或者只有多边形，亦或俩都有，不能啥都没有。
        d. 将 `1-annotation-tool/yoloe/0-generate.sh` 塞进窗口，选择了数据集后且数据集检查合规后，自动进行调用 qwen 描述
    修复：
        a. UI `dirty` + `saveIfDirty`：切图 / 切 caption 前自动 `POST /api/save`；失败则打断切换
        b. 布局改为左栏文件列表 + 顶栏/工具条 + 右栏仅目标描述 + 底栏 captions（`gd_layout_v20`）
        c. 新增 `util/validate_dataset.py` + `POST /api/validate`：必须 `images/`+`jsons/`、非空 shape；
           **整库单一几何类型**（全 rectangle 或全 polygon，混用不合格）。
           混存样例 `rm/jsons` 会失败，请改用 `jsons-det` / `jsons-segm` 或软链为 `jsons/`
        d. `POST /api/generate/start` + `GET /api/generate/status`：选目录合规后按 assess「缺啥补啥」
           后台续跑 crop→describe→caption（不 force，不覆盖已有人工结果）；顶栏「续跑生成」

18. 第十八次迭代 [v0.2.1]

    问题点：
        a. 文件又跑到左侧了，请恢复一下第十四次迭代的结果
        b. 数据集的选择有点问题，请排查
    修复：
        a. 恢复右侧子标签「目标描述 | 文件概览」，去掉左栏文件列表（保留顶栏工具/自动保存/续跑）
        b. 校验改为允许 rectangle / polygon / mixed（对齐「亦或俩都有」）；混存 crop 优先矩形防双计；
           目录浏览从当前数据集起步；校验失败不关选择框


19. 第十九次迭代 [v0.2.2]

    问题点：
        a. 缺啥补啥那里的弹窗，弹窗名设为 "正在生成标签描述和caption..."，
        b. 弹窗需要显示进度，校验通过的在后面加✅。加入每个步骤的进度条
        c. 需要支持选择 jsons/, jsons-det/，还是 jsons-sem/。主窗口显示不同的可视化框或多边形
    修复：
        a. 生成弹窗标题固定为「正在生成标签描述和caption...」
        b. 总进度条 + 每步独立进度条；通过项标 ✅；进行中步骤条动画
        c. 选数据集时可选 jsons / jsons-det / jsons-segm（jsons-sem→segm）；
           主窗口按类型可视化：det→矩形框，segm→多边形；切换标签源会强制重裁几何

20. 第二十次迭代 [v0.2.3]

    问题点：
        a. 数据集选择，分两步进行：(a) 先提示用户选择图像数据集；(b) 提示用户选择标签数据集
        b. 检测标签的属性，矩形或者多边形
        c. 按不同属性进行crop以及生成不同的可视化主窗口
        d. 左上角只点击隐藏那里，也根据标签属性进行更名，而不是"标注框"，是 "多边形" "矩形框"
    修复：
        a. 选择向导分两步：①图像数据集（images/）→ ②标签目录（任意含 LabelMe json）
        b. 选标签时自动探测 geometry=rectangle|polygon|mixed，顶栏显示属性 chip
        c. crop 按属性筛选 shape；主窗口矩形用描边框、多边形用填充轮廓
        d. 工具栏显隐按钮文案随属性变为「矩形框」/「多边形」（混合为「框/多边形」）

21. 第二十一次迭代 [v0.2.4]

    问题点：
        a. 好像现在是一定要走vllm？可以先检测 draft 完整性，若OK，则跳过 vllm，若无再进行 vllm 生成。防止离线时的 vllm 报错
    修复：
        a. assess 仅在 describe/caption 有缺口时才进 vLLM；force_crop 不再连带强制重跑 LLM
        a. crop 后重评估，draft 完整则直接跳过；仅缺描述/caption 时才 health_check vLLM

22. 第二十二次迭代 [v0.2.5]

    问题点：
        a. 选择导出 jsons-GD 时，询问用户选择导出当前文件，还是整个文件夹
        b. 刷新 caption 时，优先采用用户已选择的目标描述，只是进行句式组合的修改（这个可能后续会体现在1-annotation-tool/yoloe/rules/caption_rules.md）
    修复：
        a. 导出弹窗可选「仅当前文件」或「整个文件夹」→ jsons-GD
        b. 刷新锁定当前 caption 已选短语，仅重组句式；规则见 rules/caption_rules.md（可一起改）

23. 第二十三次迭代 [v0.2.6]

    问题点：
        a. 数据集不预设，留空，支持选择
    修复：
        a. 启动脚本默认 DATASET 为空，不传 --dataset；界面提示「选择数据集」
        a. 仍可用 bash 1-start_review.sh --dataset /path 可选预设
        a. 选目录默认从 /home/yulin 起；隐藏 . 目录与含 \ 的异常名；支持粘贴路径跳转

24. 第二十四次迭代 [v0.2.7]

    问题点：
        a. Windows 端希望本机起审阅服务，远程调用 vLLM 生成/刷新
    修复：
        a. 新增 1-start_review.bat：默认连 http://113.31.108.24:8081/v1，model=qwen3.6-35b-a3b
        a. 数据集不预设，界面选择；支持 --port / --dataset；缺依赖时自动 pip 安装
        a. 浏览起点兼容 Windows（主目录/盘符），不再写死 /home/yulin

25. 第二十五次迭代 [v0.2.8]

    问题点：
        a. 关掉启动窗口后端口仍可能被占用，希望自动释放
        b. 双击 1-start_review.bat 闪退：UTF-8（无 BOM）+ 框线/中文导致 cmd（GBK）括号解析崩坏
        c. bat 走 PATH 系统 Python，缺 flask 时也易秒退
    修复：
        a. Windows：1-start_review.bat 经 util/win_job_run.ps1 用 Job Object 托管，关窗口联动结束服务
        a. Linux：1-start_review.sh trap EXIT/INT/TERM/HUP，退出时 fuser 释放端口
        a. app.py 处理 Ctrl+C / 关控制台信号，尽快退出；启动前/退出后再兜底清端口
        b. bat / win_job_run.ps1 改为 ASCII 安全文案，避免编码拆坏脚本；默认端口 8090
        c. bat 优先 conda env yulin 的 python.exe，缺依赖再 pip；win_job_run 支持 -Python

26. 第二十六次迭代 [v0.2.9]

    问题点：
        a. 当前 win 侧打开文件夹的弹窗依然是 linux 样式，需要修改成符合 window 直觉的打开文件夹的方式
    修复：
        a. Windows：选择数据集走系统原生选夹（tkinter → 系统对话框），两步选图像目录再选标签目录
        a. 新增 /api/pick_folder；config 返回 native_folder_picker；保留「网页浏览」兜底
        a. Linux 仍用原网页目录浏览弹窗

27. 第二十七次迭代 [v0.2.10]

    问题点：
        a. 好像翻译有点慢，有没有办法提升速度。或者优先当前正在查看的图像
        b. 隐藏 "网页浏览"
        c. 点击右侧窗口的目标描述条时，能高亮图中的目标（或者其他的显眼的方式）
    修复：
        a. 翻译分两阶段（先 caption.zh，再目标短语）；切图作废旧请求、不互斥卡住；短文本加大 batch
        b. 顶栏去掉「网页浏览」；Win 仍原生选夹，Linux 仍网页浏览（均走「选择数据集」）
        c. 点击右侧目标卡片：卡片 focus + 图中对应框琥珀高亮/加粗，再点取消

28. 第二十八次迭代 [v0.2.11]

    问题点：
        a. 界面上方的 "续跑生成"，改成 "描述生成"，点击后弹窗，可选：1. 续跑补齐；2. 重新生成。然后再增加确认弹窗
        b. 界面上方的 "选择数据集" 改成 "📁选择图像数据集"
    修复：
        a. 顶栏改为「描述生成」→ 模式弹窗（续跑补齐 / 重新生成）→ confirm 确认；regenerate 强制重跑 describe+caption
        b. 顶栏文案改为「📁选择图像数据集」
        c. 顺带：describe_rules.md 原先 load 了但未进 prompt / 批量路径未传 rules，现已生效；caption_rules 本来就生效

29. 第二十九次迭代 [v0.2.12]

    问题点：
        a. 选择完目标描述后，caption 需要自动刷新
        b. 当前选择目标描述有 bug，如果点两次同一个描述，会直接删掉该描述
    修复：
        a. 选中后自动 refresh 当前 caption；取消到零选用时不重抽、不删目标
        b. 再点已选=取消选用；refresh 不得收缩 obj_ids（此前会把未选用目标挤掉导致卡片消失）；真删只能点「删除」

30. 第三十次迭代 [v0.2.13]

    问题点：
        a. 界面下方好像中文的高亮和英文的高亮不同步，这个能做到同步的么
        b. 界面下方的caption高亮模块能跟第二十七次迭代新增的右侧词条点击高亮图中目标那样，做个点击么
    修复：
        a. 高亮 span 带 data-ph-key / data-obj-id；悬停中英同 key 同步加亮（中文句搜不到短语译文时该侧无 span）
        b. 点击高亮词 → 复用 highlightObjId 画框；再点取消
    验证：
        a. 效果可用，a+b 均保留

31. 第三十一次迭代 [v0.2.14]

    问题点：
        a. 删除目标后，也自动刷新 caption。都属于更新目标描述了。
        b. 点击描述词条高亮时，可以不受隐藏标签逻辑限制，点击则高亮
        c. 可视化的外边框线宽可以再小一点
    修复：
        a. 删除目标后，对仍有 phrases 的每条 caption 自动 refresh
        b. 隐藏标签时仍可高亮：仅画出 highlightObjId 对应框
        c. 线宽 `max(1, W/700)`，高亮约 2.2×

32. 第三十二次迭代 [v0.2.15]

    问题点：
        a. 在没有隐藏标签框的情况下，能点击图中的标签框，反向高亮描述词条
        b. 新增 W 和 S 键切换前后的 caption 子标签页，并在 caption 行做提示，防止检验人员忽略了其余的标签页
        c. 界面右上角的文件名字，字体颜色用白色，显眼一些
        d. 界面上方的标签 "D:\0-data\2-DroneObject\grounding_sample\cache  jsons-det" 改为 "D:\0-data\2-DroneObject\grounding_sample\cache  图像：images  标签：jsons-det"
        e. 取消界面上方的 "矩形框" 的文字标签，保留 done:rectangle 的绿色标签
    修复：
        a. 显示标注时单击图中框 → 高亮 caption 词条 + 右侧描述卡（拖动仍平移）
        b. W/S 前后切 caption；标签行/正文提示「共 N 条 · 当前 i/N」
        c. 顶栏右侧文件名 `#status` 改为白色加粗
        d. jsons-chip 文案改为 `图像：images  标签：xxx`
        e. 去掉顶栏 geom-chip「矩形框」，保留 gen-chip `done:rectangle`；工具栏显隐按钮仍按几何显示

33. 第三十三次迭代 [v0.3.0]

    需求：
        a. Windows 同事无 Python/conda 时，也能打开检验工具（绿色便携包）
        b. 维护者一键打包，产物可直接网盘/U 盘分发
        c. 保留原生选文件夹（需 tkinter）与现有 Job Object 关窗杀进程
    实现：
        a. 便携 runtime：astral python-build-standalone + flask/pillow/tqdm/requests（不用系统 PATH）
        b. 维护者双击 `build\一键打包.bat` → 调用 `build\util\build_portable.ps1`
           产物：`build\0-dist\GroundingReview-portable-*.zip`（约 27MB）
        c. 对方解压后双击 `start.bat`，无需装 Python（说明见 `README.txt`）
        d. 本机布局：`build\runtime` / `build\cache` / `build\0-dist` /
           `build\util\build_portable.ps1` / `build\util\start_portable.bat` / `build\user_readme.txt`
        e. `1-start_review.bat` Python 查找顺序：zip 内 `runtime\` → 本机 `build\runtime\` → conda yulin → PATH
        f. `.gitignore` 忽略 `build\runtime` / `build\0-dist` / `build\cache`
    说明：
        a. 官方 python.org 静默安装在部分机器 TargetDir 落盘失败，故改用 standalone 压缩包
        b. 首次打包需联网下载 Python；再次打包可复用 `build\runtime`（`-SkipPythonInstall`）
        c. 同事使用 zip 时不需要再下载运行时

34. 第三十四次迭代 [v0.3.1]

    需求：
        a. 「📁选择图像数据集」 的左侧，新增下拉 「💫多模态模型服务」
        b. 模型服务在 `model\server.json` 配置（多服务 + `default`）
        c. 自动检测服务是否可用：可用绿色小点，不可用红色小点
    实现：
        a. 顶栏下拉在「选择图像数据集」左侧；选项前缀 🟢/🔴，旁侧圆点同步状态
        b. `util\servers_io.py` 读配置；`/api/model_services?probe=1` 并行探测 `/v1/models`
        c. 切换服务 → `/api/model_services/select` 更新 `state.base_url/model`；启动时用 JSON 默认项
        d. 每 60s 复检；便携包打包时拷贝 `model/`
    配置示例：
        `services[{id,name,base_url,port,model,default}]`，`default:true` 建议仅一项

35. 第三十五次迭代 [v0.3.2]

    问题点：
        a. 🟢/🔴 图标太大了，有没有小一点的，美观一点；下拉框左侧和选项里面都使用 6px 旁侧圆点（绿/红/灰）
        b. 「💫多模态模型服务」选择完成后，悬停看全，右侧不再显示更多
    修复：
        a. 自定义下拉：按钮左侧 + 每条选项内均为 6px 圆点（绿/红/灰），去掉大 emoji
        b. 选中后顶栏只保留服务名；`name` / `base_url` / 状态放在悬停 title

36. 第三十六次迭代 [v0.3.3]

    需求：
        a. 选择标签时，也支持 xml 的 VOC 格式的标签（一般为 bbox 矩形框）：参考 coco128/annotations
    实现：
        a. common.load_voc_xml：VOC object/bndbox → LabelMe rectangle shapes
        b. load_labelme 自动识别 .xml/.json；校验/裁剪/列 stem 统一加载
        c. 标签目录候选增加 annotations / xmls / voc；UI 文案同步
        d. 空目标 VOC XML 跳过不报错；几何默认 annotations/xmls/voc → 矩形框

37. 第三十七次迭代 [v0.3.4]

    需求：
        a. 一键打包加速（runtime 已存在时不要每次都慢）
    实现：
        a. 依赖已齐则跳过 pip（`-ForcePip` 可强制重装）
        b. 大目录拷贝改 robocopy；清理用 rmdir
        c. 压缩用 .NET Fastest（本机 ~2s；略大于 Optimal），不再用 Compress-Archive / 慢 tar
        d. 复用 runtime 时整包目标约十几秒（拷贝 + 快压）

38. 第三十八次迭代 [v0.3.5]

    需求：
        a. 界面右侧窗口的大小，可以再拉宽一些，因为主窗口很大，右侧窗口如果描述词长的话，不美观
    实现：
        a. 右侧栏默认宽 300 → 420；拖拽上限 520 → 720（下限 240）
        b. 布局缓存升到 gd_layout_v22；旧默认 300 不继承，手动拉过的宽度仍保留

39. 第三十九次迭代 [v0.3.6]

    问题点：
        a. 同一目标多条描述短语时，caption 会写成「两人」（如 lady holding umbrella stands near woman in floral swimsuit）
    修复：
        a. caption_rules.md：同 obj_id 必须合成一个主体；禁止 near/beside/next to 拆人；补易错示例
        b. caption.py：prompt 按 obj_id 分组标注；fallback 模板对同目标也不再用 near 拼多人

40. 第四十次迭代 [v0.3.7]

    问题点：
        a. 好像图像路径固定要 images，可以修改得和标签路径一样灵活么
    实现：
        a. 新增 `images_dirname`（相对名 / `.` / 绝对路径），与 `jsons_dirname` 对称
        b. 校验、裁剪、读图、draft/config 全链路透传；默认仍为 `images` 兼容旧数据
        c. 选夹支持 images / JPEGImages / 任意含图目录，或数据集根下自动识别图像子目录
        d. 顶栏芯片显示「图像：xxx  标签：yyy」

41. 第四十一次迭代 [v0.3.8]

    问题点：
        a. W / S 不可以首尾相接，这样检验人员可能不知道已经全部 caption 都校验完了，导致重复校验。就切到底不给切了就行，不用其他提示
    实现：
        a. `navCaption`：首尾钳位，不再取模循环；到边界再按 W/S 无动作、无额外提示

42. 第四十二次迭代 [v0.3.9]

    问题点：
        a. 右侧未选目标描述时，caption 仍可能残留不可控文本；切换 caption 应提醒
        b. 不同 caption 命中同一目标时，选用/刷新描述重复度偏高
        c. 刷新描述时，当前 caption 已选的目标描述尽量不要被刷掉，可以刷剩余的 1-2 个描述
    实现：
        a. 无选用短语 → caption/zh 强制为空（前端取消选用、渲染归一化、保存与 refresh 后端一致）
        b. 切换 caption（W/S 或点 tab）时，若离开的那条无选用 → 弹窗「caption 为空，请选择目标描述」
        c. 生成/新增/重抽 caption 时，按 obj 避开其它 caption 已用短语
        d. 刷新目标描述时把已用短语传给模型并过滤；右侧芯片对「他用」短语虚线样式 + title 提示
        e. 刷新描述：保留当前 caption 已选短语，只重刷其余空位（前端传 sentence_id；已选复用原中文）
        f. 切换/展示当前 caption 时，已选用短语立刻出现在右侧并绿色高亮（缺条目则补进 descriptions）

43. 第四十三次迭代 [v0.3.10]

    问题点：
        a. 每个芯片只固定显示 3 个描述；当前 caption 描述为亮绿色
        b. caption 需要根据目标描述来生成，避免出现目标1+目标2，但是 caption 描述是 "目标1也是目标2" 这种歧义的句子
    实现：
        a. 保留：右侧每个目标固定展示最多 3 条（已选优先）；生成/刷新也收敛为 3 条
        b. 回退：亮绿色选中态（仍用原绿色 selected）
        c. 修复「点不掉描述」：同步逻辑不再从 caption 正文反推选用；取消选用按规范化短语匹配
        d. caption 规则/prompt：不同 obj_id 禁止 is/is also/who is also 合并；同 obj 才可用同位；补易错示例

44. 第四十四次迭代 [v0.3.11]

    问题点：
        a. 大数据集每次启动生成都全量读标签做「格式校验」，看起来卡在校验阶段
        b. 已开一个网页后再双击 bat，会 Free-Port 杀掉旧实例，无法多开
    实现：
        a. 校验结果写入 `draft/validate_log.json`；签名（标签文件名/size/mtime + 图片数）未变则复用，跳过全量解析；「重新生成」force 时强制重扫
        b. `win_job_run.ps1`：首选端口占用则自动 PORT+1…，启动不再杀其它监听；关窗只释放本实例端口；bat 用实际端口开浏览器
        c. 关浏览器标签：`/api/shutdown` 短延迟退出释放端口；心跳仅用于取消（刷新不误杀），不做超时强关
        d. 目标描述刷新：同一次视觉调用带回中文；缺译改批量翻译；按钮即时「刷新中…」反馈

45. 第四十五次迭代 [v0.3.12]

    问题点：
        a. 浏览器标签仍为默认地球图标，品牌感弱；带深色底的小图标在标签栏不清晰
        b. 标注规则依赖钉钉外链配图，离线/同事协作不便
        c. draft/caption 已齐套时，打开数据集仍卡在校验/评估几十秒
    实现：
        a. Favicon 定稿：双色 G 字标（浅紫弧 + 靛蓝横杠 + 绿点），透明底、加大描边；横杠上移以免小尺寸误读为 C；`util/static/favicon.svg`
        b. 标注规则配图落盘 `z-others/pic/`，文档迁至 `z-others/标注规则.md`；便携包可打入规则与配图
        c. 打开加速：校验签名改 scandir 轻量指纹；续跑 `assess_draft(deep=False)` 只做文件存在性；`/api/images` 不再逐张读 caption
        d. 选夹/浏览加速：`dir_has_*` scandir 早停；浏览 light 不扫任意子目录、不解析几何；确认几何仅抽 5 个样本

46. 第四十六次迭代 [v0.3.13]

    问题点：
        a. 右侧窗口标签描述，描述词点亮和点灭后，顺序尽量不要跳动
        b. 而且点击时好像有延时反馈，导致点多两次，影响效率
    实现：
        a. 右侧芯片按 descriptions 原顺序展示；点亮/点灭不再把已选顶到最前；缺条目补在末尾
        b. 选用即时改状态并高亮；caption LLM 刷新改为防抖后台执行，避免等刷新时连点反选

47. 第四十七次迭代 [v0.3.14]

    问题点：
        a. F5 刷新网页后回到第一张图，打断审阅进度
        b. F5 / 关标签会触发 `/api/shutdown`，心跳取消竞态导致误杀进程、端口被释放
    实现：
        a. sessionStorage 记住当前数据集 root / stem / activeCap；刷新后同数据集则恢复当前位置
        b. 去掉网页心跳与关页 shutdown；仅关闭 bat/控制台或 Ctrl+C 释放端口（Job Object 逻辑不变）

48. 第四十八次迭代 [v0.3.15]

    问题点：
        a. caption 更新/翻译回写时会改掉右侧目标描述的选用状态
    实现：
        a. 前端 mergeCaptionsKeepSelection：服务端只更新 caption 正文与 zh，phrases/obj_ids 以本地选用为准
        b. 后端刷新锁定已选短语（obj_id 强转 + 对齐 descriptions）；已有 phrases 列表时禁止按 obj 重抽

49. 第四十九次迭代 [v0.3.16]

    问题点：
        a. 目标描述短语常串类（如 car 写成摩托车；specialvehicle 未按特种车辆写）
    实现：
        a. `describe_rules.md` / vesthalmet：强化类别锁定、标签优先、串类反例；图与 label 冲突以 label 为准
        b. `describe.py`：prompt/system 强制 GROUND TRUTH class；粘连标签给 natural reading 提示

50. 第五十次迭代 [v0.3.17]

    问题点：
        a. draft 已齐套仍「不能秒开」：续跑每次仍全量 scandir 标签做指纹 / listdir 标签评估
        b. 已选「全图 / 跟目标」后，A/D 切图会跳回默认全图
    实现：
        a. validate：有合格 validate_log + draft 齐套时 skip-scan 直接复用（重新生成仍 force 全量）
        b. assess_draft(deep=False)：以 draft/objects 为宇宙，不再扫全量标签目录
        c. loadStem 保留 fitMode；仅清除手动视图，按当前模式适配新图

51. 第五十一次迭代 [v0.3.18]

    问题点：
        a. 右侧 5 条选用短语都在 caption 里，但正文只高亮链接 4 条
        b. 短串落在长串内时 tokens_positive 可能指错（首次 find）
    实现：
        a. assign_phrase_token_spans：最长优先、互不重叠、每条一个区间；缺则句末补写
        b. 高亮 / 保存 / 导出 / ensure_phrases_in_caption 统一走该分配
        c. 中文 caption 仅展示翻译，不做短语高亮链接（只保留英文高亮）

52. 第五十二次迭代 [v0.3.19]

    问题点：
        a. 右侧已选 5 条描述，自动生成的 caption 有时只有 4 条，需手动刷新 caption
    实现：
        a. 刷新锁定改用完整 caption.phrases（不再被 desc_map 滤掉已选）
        b. 自动刷新：请求期间若选用变了则重刷；合并后 ensureLocalPhrasesInCaption 补漏短语
        c. 乐观追加短语改为非重叠判定，避免短串误判已在句中

53. 第五十三次迭代 [v0.3.20]

    问题点：
        a. 首先，描述词扩展成六个，可以避免频繁刷新（这个时候右侧窗口的固定大小可以再扩大一些，保证能一行放下）
        b. 用户刷新时，根据已选的描述来刷新，用户从6个描述中选择一个，但是还想刷新。说明这个最接近特征，还害不够贴切，此时，2个描述词围绕这个描述重新生成，其余3个描述词还是原来的逻辑。
    实现：
        a. 审阅侧 / 批量 describe 默认 6 条；右侧面板默认宽 600（可拖到 960）
        b. 有已选刷新：保留已选 + seed 围绕生成 ≤2 条贴近变体 + 其余空位原 avoid/多样化逻辑（1 已选 → 1+2+3）
        c. 无已选：整组重刷 6 条；围绕不足时把空位并入多样化调用

54. 第五十四次迭代 [v0.3.21]

    问题点：
        a. 右侧取消描述再选另一条时，caption 有时仍保留旧短语，新旧两条并存
    实现：
        a. save 竞态：editRev，保存中途有新编辑不误清 dirty；刷新前强制 persist
        b. refresh_caption 接受 body.phrases（前端选用优先于磁盘）
        c. pruneCaptionToSelectedPhrases：合并/换选时清掉未选用候选残留

55. 第五十五次迭代 [v0.3.22]

    问题点：
        a. 自动/手动刷新 caption 后正文被抠烂（如 red sedan → red …, with red sedan）
        b. 下方「刷新本条」缺少刷新中反馈
        c. 空 caption 提示确认后仍切到下一条
    实现：
        a. prune/strip 与已选短语联合分配 span：未选短词嵌在已选长词内时不删
        b. 按字符区间删除，禁止 naive split 误伤
        c. 刷新后若本地又改了正文则再 persist
        d. 「刷新本条」显示刷新中…并禁用；caption 区淡显；自动刷新共用同一反馈
        e. 空 caption 弹窗确认后停留本标签，不切换（W/S / 点标签同理）

56. 第五十六次迭代 [v0.3.23]

    问题点：
        a. 右侧目标描述末尾增加「保留源标签」：选用后该目标只用源标签参与 caption，不可再多选描述词
    实现：
        a. 末尾紫色虚线芯片「保留源标签」；选用后清掉该目标其它描述，phrase 带 keep_source_label
        b. 与描述词互斥：点描述词则退出源标签模式；源标签不写入 descriptions
        c. caption 刷新透传 keep_source_label；描述刷新不把源标签当 keep/around 种子
        d. caption 正文用标签自然语言（specialvehicle → special vehicle）

57. 第五十七次迭代 [v0.3.24]

    问题点：
        a. 通用 describe 规则补充方位 / 部分特征，并约束短语 1–2 维组合
        b. 仅改 md 时刷新描述几乎无感：硬编码 cover_line 仍只要求子类/外观/动作
        c. 刷新描述强制凑满 6 条才落盘，返回不足就重试/失败
    实现：
        a. `describe_rules.md`：方位、部分特征；风格表增加串类反例与描述组合
        b. `describe.py`：SYSTEM_PROMPT / cover_line 对齐五轴与 1–2 维组合
        c. 提示改为 up to N；≥1 条即可落盘（有多少更新多少，上限仍 6）

58. 第五十八次迭代 [v0.4.0]

    需求：
        a. 校验页面以图像文件为准，如果图像文件已经不存在，则不再加载空的标签，而且总校验文件也更改为实际的图像文件数
        b. 界面下方末尾新增 [删除全部]，可一键删除全部的不合适caption并弹窗提示
    实现：
        a. `list_stems` / 审阅列表：objects ∩ 现存图像；缺图 draft 不再进入列表与进度总数
        b. `validate_dataset`：无对应图像的标签直接跳过不报错；stats 增加 n_images，齐套门槛按图/标签交集
        c. crop 写 objects.jsonl 时同样过滤缺图 stem
        d. caption 栏末尾「删除全部」+ 确认弹窗；`/api/delete_all_captions/<stem>` 清空当前图全部 caption

59. 第五十九次迭代 [v0.4.1]

    需求：
        a. 快捷键：Q[全图] E[跟目标] F[多边形/标注框]
        b. 右侧窗口，若用户没有选择描述标签，则用红色高亮（和点击高亮不冲突），并在切换 caption 或者切图时提示当前caption还有未处理目标
    实现：
        a. 审阅页 keydown：`Q`→`setFitMode('full')`，`E`→`setFitMode('zoom')`，`F`→`toggleBoxes()`（输入框内不触发）
        b. 工具栏提示与 README 快捷键表同步补充
        c. 关联目标无选用描述 / 源标签 → 卡片红色 `pending` +「未选」角标；点击高亮仍用黄色 `focus`，可叠加
        d. 切 caption / 切图：若当前 caption 仍有 pending 目标则弹窗拦住并列出 obj id

60. 第六十次迭代 [v0.4.2]

    需求：
        a. [删除全部] 用紫色来显示，参考 [保留源标签]
        b. 右侧窗口不要再给出虚线的描述，直接刷新不给重复的
    实现：
        a. 「删除全部」改 btn-outline-purple（紫色虚线，对齐保留源标签）
        b. 右侧不再渲染 used-elsewhere 虚线芯片；他用短语直接隐藏
        c. 展示 / 刷新回写均过滤其它 caption 已用短语；刷新仍走后端 avoid，前端再滤一层

61. 第六十一次迭代 [v0.4.3]

    需求：
        a. [删除全部] 用紫色来显示，参考 [保留源标签]
        b. 右侧窗口不要再给出虚线的描述，直接刷新不给重复的
    实现：
        a. 「删除全部」改 btn-outline-purple（紫色虚线，对齐保留源标签）
        b. 右侧不再渲染 used-elsewhere 虚线芯片；他用短语直接隐藏
        c. 展示 / 刷新回写均过滤其它 caption 已用短语；刷新仍走后端 avoid，前端再滤一层
    发布：
        a. 便携包装入 v0.4.2：删除全部紫色样式 + 去虚线重复描述

62. 第六十二次迭代 [v0.4.4]

    需求：
        a. 描述词最多给5个
        b. 刷新时，"person in red helmet" 和 "person wearing red helmet" 不要同时出现，尽量是不同维度的组合描述（同时记得更新rules）
        c. 同组再滤同义名词（如 helmet 与 hard hat 不并列）；同 caption 宜为不同维度（helmet / shirt / standing）
        d. 存量跨 caption 重复：未选他用（精确）隐藏；已选他用虚线保留便于点灭；含虚线已选合计最多 5；近义他用后续再议
        e. 切换 caption 后，已选描述词不应跳到末尾，应保持不动
        f. 鼠标左键选中标签卡片后，右键可以刷新
    实现：
        a. 展示/刷新上限 →5（PHRASE_DISPLAY_LIMIT / DISPLAY_N / min_phrases；rules/README 同步）
        b. phrase_content_key / phrase_synonym_key：同组近义不并列；prompt + rules 同步
        c. 点亮/点灭/切 caption：按 descriptions 原序；滑动窗口盖住已选（硬顶 5），禁止抽到末尾
        d. pickDisplayPhrases：未选+精确他用隐藏；已选他用虚线；不落盘删除
        e. sync 仅追加真正缺失的已选；刷新时已选留在原下标、新词填空位
        f. 目标卡左键选中后右键触发 refreshObjectDescription（未选中右键仅高亮）
    发布：
        a. 便携包 GroundingReview-portable-v0.4.4.zip

63. 第六十三次迭代 [v0.4.5]

    需求：
        a. 刷新描述加速：去掉串行双次模型调用与刷新路径阻塞翻译
    实现：
        a. refresh_desc：有已选也只打 1 次视觉（seed+avoid）；with_zh=False；max_retries=1
        b. 缺中文不在刷新接口里 translate_batch；先回英文，前端 fillDescZhAsync / ensure_zh 补
        c. 默认 VersionTag → v0.4.5
        d. 切图 ensure_zh 失败时：接口仍带回 item；前端状态栏展示具体 error，不阻断英文审阅
    发布：
        a. 便携包 GroundingReview-portable-v0.4.5.zip
